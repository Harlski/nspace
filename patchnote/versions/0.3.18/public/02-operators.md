# Public patch notes — operators (`0.3.18`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, backups, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- **[OPS]** **Client-only** release — deploy an updated **client** bundle (or full image if you ship client+server together). No server binary changes, migrations, or new environment variables for this version.
