# Public patch notes — operators (`0.3.6`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

_(Draft — not published.)_

- No new environment variables, Docker/compose changes, or migrations for this set of client UI fixes.
- **Support note:** Header marquee “read once then hide ~10m” is **client-only** (`localStorage` key `nspace.headerMarquee.newsSuppress`); nothing to configure on the server beyond existing marquee payload behavior.

- _Deploy / config deltas vs previous patch-notes version._
