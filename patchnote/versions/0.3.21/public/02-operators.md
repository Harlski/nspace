# Public patch notes — operators (`0.3.21`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- **[OPS]** Ship **client and server** together for this release so prefab placement scope, extra-floor validation, the 12-character design name limit, and floor brush strokes (`brushSize` on `placeExtraFloor`) stay aligned. No new env vars or migrations.
