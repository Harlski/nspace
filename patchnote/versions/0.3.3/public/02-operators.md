# Public patch notes — operators (`0.3.3`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- No new required environment variables or Docker compose changes for this release; deploy flow matches the previous patch. If you customize **Vercel** rewrites for admin pages, keep `/admin/header` pointed at your API host as already documented for the header marquee.
