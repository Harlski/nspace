# Public patch notes — operators (`0.3.14`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- **[CHANGE]** No new **environment variables**, compose services, or deploy steps for this release.
- **[CHANGE]** Server accepts **`placeTeleporterBidirectionalPair`** for same-room linked teleporters (existing deploy: update game server image/build with client).
