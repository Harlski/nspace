# Public patch notes — operators (`0.3.20`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- **[OPS]** Ship **client and server** together for this release so prefab placement scope, extra-floor validation, and the 12-character design name limit stay aligned.
- No new environment variables, compose profiles, or database migrations for these changes.
