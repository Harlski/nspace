# Public patch notes — operators (`0.3.26`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- **[OPS]** Client-only release — rebuild and redeploy the **client** bundle (or full game-server image if you ship static assets from it). No server env, Docker compose, or migration changes.
- No new or removed environment variables.
