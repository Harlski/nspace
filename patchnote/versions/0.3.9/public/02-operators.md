# Public patch notes — operators (`0.3.9`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- [OPS] **No deploy or config change** for this release.
- [CHANGE] **`/patchnotes`** may list a **Hotfix** audience tab for a semver when `04-hotfix.md` is present for that version (client build bundles markdown at compile time).
