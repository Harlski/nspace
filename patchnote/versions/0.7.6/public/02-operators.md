# Public patch notes — operators (`0.7.6`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

No deploy, env, Docker, or migration changes in this build. Ship the usual client + server images together so Private Room spawn and Pixel Collaborator occupancy stay in lockstep.
