# Public patch notes — operators (`0.3.24`)

**Audience:** self-hosters, deployers, infra — Docker, env vars, migrations, breaking ops changes.  
**Depth:** concrete steps, new/removed variables, compose profiles, backup/restart expectations.

---

- No deploy, env, or migration changes for this release. New chat-report tickets may include optional `reportContext` in `server/data/feedback/tickets.json` (same **`FEEDBACK_STORE_FILE`** as before).
