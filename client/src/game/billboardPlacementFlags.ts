/**
 * Temporary: disable choosing 2×1 in the billboard modal (button disabled) and clamp
 * placement draft to horizontal. Match `BILLBOARD_VERTICAL_PLACEMENT_TEMP_DISABLED` in
 * `server/src/rooms.ts` for API enforcement.
 */
export const BILLBOARD_VERTICAL_PLACEMENT_TEMP_DISABLED = true;
