/** Matches server `TELESCOPE_ACHIEVEMENT_ID` in onboardingComplete.ts. */
export const TELESCOPE_ACHIEVEMENT_ID = "telescope";

/** Zoom in/out duration while Telescope is held or returning (ms). */
export const TELESCOPE_HOLD_ZOOM_MS = 225;
