/**
 * Publication version string for bundled Terms (/tacs) + Privacy (/privacy).
 * Bump when either document changes materially; wallets must acknowledge the new version.
 * Keep in sync with `client/src/termsPrivacyVersion.ts` and prose under `client/src/termsPrivacy/content/`.
 */
export const TERMS_PRIVACY_DOCS_VERSION = "2026-05-12";
