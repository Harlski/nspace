# Reason 519027 — THE-LARGER-SYSTEM: payment intent proxy + payer scope

## Why `docs/THE-LARGER-SYSTEM.md` was updated

The monetization subsection had become **stale**: it still claimed the game server did not expose JWT routes to the sidecar, after we shipped that layer. The doc is normative for direction; it must match shipped behavior and the remaining gap (entitlements / product state).

## What changed

- Recorded **JWT `/api/payment/*` proxy** and **payer-scoped** sidecar `GET`/`verify` as shipped.
- Clarified the **remaining** boundary: **no game-server entitlements** yet (username registry, etc.).
- Tightened **constraints**: `payerWallet` on proxy paths always from **`sub`**.

## How this serves the larger system

- **Coherence:** One source of truth for “HTTP done vs. gameplay hooks pending.”
- **Security narrative:** Payer scoping + JWT proxy are part of the agreed architecture, worth preserving in THE-LARGER-SYSTEM so future features do not bypass them.
- **Evolvability:** Next work is clearly “consume `verify` success in authority stores,” not more plumbing discovery.
