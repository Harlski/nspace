# Feature checklist

Inventory of major areas as implemented in the repo. Use checkboxes for tracking; not all items may need to stay checked in every fork.

## Payment intents (optional microservice)

- [x] Workspace [`payment-intent-service`](../payment-intent-service/) — Express HTTP API, SQLite ledger, Nimiq `getTransaction` verification for incoming NIM to a configured hot wallet
- [x] Docker image + Compose service `payment-intent` (profile **`payment`**)
- [x] Pluggable **`featureKind`** handlers (`nspace.test.min`, implemented **`nspace.billboard.slot`** for paid Hub billboard campaigns, **`nspace.cosmetic.unlock`** for cosmetic shop purchases, + reserved stubs for usernames, land, teleporter — see `src/features/builtin.ts`)
- [x] **`/advertise`** main-site dashboard + `GET/POST /api/advertise/campaigns`, visibility economics (**400 NIM ≈ 24h** on-screen at full audience, ~14-block view radius; **no minimum fund** — prepaid time scales as `fundNim ÷ rate`; dwell 10/30/45s per slide in rotation sets), fund amount + duration estimate (`GET /api/advertise/estimate`), payment intent proxy, **Fund → Pay with wallet** (Nimiq Pay `sendBasicTransactionWithData` or Hub `checkout` after intent create; manual recipient/memo fallback), server-side payment sync (`POST …/sync`), admin approve/reject (`/admin/campaign`, `POST …/approve`, `POST …/reject`), optional Telegram ping on funded campaigns — approved campaigns are added to admin **rotation sets** and placed in-world by admin via build **Campaign** tab ([server/src/advertisePage.ts](../server/src/advertisePage.ts), [server/src/adminCampaignPage.ts](../server/src/adminCampaignPage.ts), [server/src/rotationSetStore.ts](../server/src/rotationSetStore.ts), [server/src/campaignFulfill.ts](../server/src/campaignFulfill.ts), [server/src/campaignVisibilityEconomics.ts](../server/src/campaignVisibilityEconomics.ts))
- [x] `/admin/system` — sidecar health with **green / yellow / red** status: **Payment intent** (`PAYMENT_INTENT_SERVICE_URL` + optional `PAYMENT_INTENT_API_SECRET` probe) and **Payout** (`PAYOUT_SERVICE_URL` + `PAYOUT_SERVICE_API_SECRET` probe); degraded/down states show probe detail and a `docker compose logs …` hint ([`server/src/paymentIntentProbe.ts`](../server/src/paymentIntentProbe.ts), [`server/src/payoutServiceProbe.ts`](../server/src/payoutServiceProbe.ts))

## Auth and session

- [x] `GET /api/auth/nonce` — challenge nonce
- [x] `POST /api/auth/verify` — Nimiq signed message → JWT + address (or dev bypass); first-time wallets must send `acceptedTermsPrivacyVersion` matching server `termsPrivacyVersion` until ack is recorded in server data (`terms-privacy-acceptance.json`; optional **`TERMS_PRIVACY_ACCEPTANCE_STORE_FILE`** / legacy **`LEGAL_CONSENT_STORE_FILE`**)
- [x] WebSocket auth via `token` query param
- [x] Client session cache (localStorage) for token + address
- [x] **Username prompt at login** — wallets without a custom username see a picker after sign-in (before entering the world); **5** optional **Not now** deferrals per account (`usernamePromptDeferCount` in `player-profiles.json`), then the prompt is mandatory; deferrals reset when an admin **clears** a username; **username-set ban** skips the prompt; `GET /api/player-profile/username-prompt`, `POST /api/player-profile/username/defer`, `PUT /api/player-profile/username` (first set allowed even when self-service is off); auth verify may include `usernamePrompt`; alphanumeric **1–12** chars, **bad-words** profanity filter + reserved-name list (`server/src/usernamePolicy.ts`), **24h change cooldown** only while a custom username is already set; self-service toggle in **`/admin/settings`** (default **on**)
- [x] Dev login path when `DEV_AUTH_BYPASS=1` on server and `VITE_DEV_AUTH_BYPASS=1` on client

## Multiplayer

- [x] `welcome` with self, others, room id, bounds, doors, obstacles, extra floor
- [x] **`chatBacklog` on `welcome`** — bounded per-room buffer of recent non-bubble `chat` lines (reconnect / room switch)
- [x] Periodic `state` / `stateDelta` with players’ positions and velocities (server tick; delta when only a subset changed)
- [x] `playerJoined` / `playerLeft`
- [x] `chat` broadcast with rate limit; server-side **profanity censor** (shared word list with usernames + custom terms); all-profanity messages rejected with `chat_blocked_profanity`
- [x] **Admin chat log** — `/admin/chat` + `GET /api/admin/chat` / `GET /api/admin/chat/message` (system admin): global / per-room / per-user search (7d default, 30d max), censored text with optional original + live/backlog audience; inline channel mute + feedback link ([server/src/adminChatLog.ts](../server/src/adminChatLog.ts), [server/src/adminChatPage.ts](../server/src/adminChatPage.ts), [server/src/profanityFilter.ts](../server/src/profanityFilter.ts))
- [x] **`serverNotice` / `restart_pending`** — game-admin **`POST /api/admin/announce-restart`** (`etaSeconds` 5–7200, optional `message`) broadcasts a maintenance countdown on **all** game WebSockets, then exits via the normal shutdown path; optional **`POST /api/hooks/pre-deploy-restart`** (Bearer **`DEPLOY_RESTART_HOOK_SECRET`**, same body) for **GitHub Actions → VPS** deploy ([`.github/workflows/deploy-docker.yml`](../.github/workflows/deploy-docker.yml): 60s notice + wait before `docker compose stop`); client **HUD banner** + friendlier **disconnect** status line ([server/src/index.ts](../server/src/index.ts), [server/src/config.ts](../server/src/config.ts), [server/src/rooms.ts](../server/src/rooms.ts), [client/src/net/ws.ts](../client/src/net/ws.ts), [client/src/main.ts](../client/src/main.ts), [client/src/ui/hud.ts](../client/src/ui/hud.ts))

## Rooms and travel

- [x] **Hub** (`chamber` id — central social home) and **Commons** (`hub` id — open communal build) with distinct base bounds
- [x] **Pixel** (`pixel`) — **500×500** floor-only paint board (full global grid); Hub north door `(0, 12)`; no object placement; no NPC wanderers; floor recolor within a **square** **`PLACE_RADIUS_BLOCKS`** zone (±9 tiles on X and Z from the player's tile; default 9); admins may set room **background hue/neutral** via the hue ring; **neutral gray** implicit base floor (only painted tiles persisted/synced); join loads **nearby chunks only** (~32×32 tile regions), not the full grid; **forward-only paint log** (`server/data/pixel/paint-log.jsonl` — baseline snapshot + ordered `paint` lines with `colorRgb` for future timelapse); public live snapshot **`GET /pixels.png`** (500×500 PNG, 1 px per tile, cached, `ETag` / short `max-age`) ([server/src/pixelPaintLog.ts](../server/src/pixelPaintLog.ts), [server/src/pixelBoardImage.ts](../server/src/pixelBoardImage.ts), [server/src/roomLayouts.ts](../server/src/roomLayouts.ts), [server/src/rooms.ts](../server/src/rooms.ts), [server/src/blockColors.ts](../server/src/blockColors.ts))
- [x] **Stream cinema** — `?stream=1` hides HUD, forces continuous render, **top-down** view that **fills the window** with crisp tiles and **slow pan** across large maps; **observer-only** (not listed as a player, no movement or floor edits); **allowlisted wallets only** (`STREAM_OBSERVER_ADDRESSES` env and/or **`/admin/settings`** — rejects unauthorized `?stream=1` with WS close **4403**); top-left **Nimiq Space** broadcast overlay (“Play Nimiq Space at https://nimiq.space”); player **identicons only** (no name pills), **3×3 floor tiles** in top-down overview (smoothly back to **1×1** when spotlight zooms to a player); `?streamFollow=1` cycles overview ↔ random player spotlight (~30s **isometric** follow with smooth top-down ↔ iso zoom transition); `?noScroll=1` disables overview pan (fixed framing); `?streamChat=1` shows chat; `?streamDebug=1` on-screen pan tuning (speed + X/Y edge insets); `?room=pixel` join hint; default room `pixel` when `stream` without `room` ([client/src/main.ts](../client/src/main.ts), [client/src/stream/streamDirector.ts](../client/src/stream/streamDirector.ts), [client/src/stream/streamPanDebug.ts](../client/src/stream/streamPanDebug.ts), [client/src/game/Game.ts](../client/src/game/Game.ts), [client/src/ui/hud.ts](../client/src/ui/hud.ts), [server/src/index.ts](../server/src/index.ts), [server/src/config.ts](../server/src/config.ts), [server/src/rooms.ts](../server/src/rooms.ts))
- [x] **Spatial tile sync (large rooms)** — rooms ≥10k tiles (e.g. Pixel 500×500): server sends floor/block deltas only inside the client’s view-interest chunks (32×32 tiles); **non-admins** capped to **zoom 22.92** (~**±27 tiles** view interest + subscriptions); **admins** and **stream observers** may request wider regions; client reports camera bounds via `setViewInterest`; welcome payload is chunked; incremental floor mesh updates avoid full-room rebuilds on single-tile edits ([server/src/interestChunks.ts](../server/src/interestChunks.ts), [server/src/rooms.ts](../server/src/rooms.ts), [client/src/game/interestChunks.ts](../client/src/game/interestChunks.ts), [client/src/game/Game.ts](../client/src/game/Game.ts))
- [x] Doors on base grid; client reconnects with spawn hint when crossing
- [x] **Session resume** — on login / **Reconnect**, server `resume=1` restores last room + walkable tile if disconnect was **≤ 10 minutes** ago (`player-last-sessions.json`); invalid/deleted room or unwalkable tile → **Hub** default spawn; intentional travel (doors, Return to Hub, idle Hub return) still sends explicit room + `sx`/`sz`
- [x] “Return to hub” when not in hub (client-driven room change)
- [x] **Wallet rooms:** configurable default **entry spawn** (persisted in `rooms.json` v6+); used when a player has no saved position in that room; `updateRoom` with `joinSpawn`, `welcome` / `roomJoinSpawn` wire updates; build-mode floor ring + **Entry spawn** tool ([server/src/roomRegistry.ts](../server/src/roomRegistry.ts), [server/src/rooms.ts](../server/src/rooms.ts), [client/src/main.ts](../client/src/main.ts))

## Movement

- [x] `moveTo` — server pathfinding over walkable tiles, obstacles blocking non-passable cells
- [x] Path queue + movement tick (`MOVE_SPEED`, arrival epsilon)
- [x] Walk bounds expanded when extra floor exists (see `walkBounds` in rooms)

## Building — blocks / obstacles

- [x] `placeBlock` / `setObstacleProps` — passable, half, quarter, hex (**thickness** stepper 25–100% — thinner prism, same tile footprint), **pyramid** (+ base scale), **sphere** (**size** stepper 25–100%), **plain cube rotation** (`cubeRotX` / `cubeRotY` / `cubeRotZ` 0–3 = 90° steps per axis — visual only; dock **Rot X/Y/Z** steppers, **↺ ↻** or **R** for Y), **ramp** (+ direction), **`colorRgb`** (build dock **hue ring** + custom hex; legacy `colorId` migrated on load), and related props (see `PlacedProps` / `TerrainProps` in [server/src/rooms.ts](../server/src/rooms.ts))
- [x] `removeObstacle`, `moveObstacle` (props preserved)
- [x] Client: build mode (`B`), placement bar, object edit panel, reposition flow — **translucent hover ghost** for valid move targets (gates: ghost + exit/front tints + frozen source opening; billboards: footprint + interact ghost; other obstacles: `makeBlockMesh` ghost; see [docs/THE-LARGER-SYSTEM.md](THE-LARGER-SYSTEM.md)); **HUD:** no full-height collapsible right mode rail (**Feedback** on the self **profile** card footer; **Build** toggle in a compact bottom-right strip); **bottom build dock** (dark glass chrome; see [build_menu.md](build_menu.md)): **Terrain / Props / Buildings** when scope is **Objects**; **Objects / Room** on the **right** of the dock tab row (`<select>` on desktop, **overlay list** on coarse-pointer mobile); **closing build or reopening** resets scope to **Objects** when both caps apply; **Room** scope uses **Floor** / **Room settings** tabs (floor tool + **floor tile hue ring** in the dock **context** column on Floor — **hover preview** and **placed tile color**; **room background hue** on Room settings); tools + **hue ring** + **off-screen placement/selection 3D preview**; placement bar host **off-dock**; **block height / pyramid base** in dock **context** (**two columns**: modifiers left, hue right); **live “Selected”** GL preview **docked** across object categories; **Terrain** shape cards (**Cube / Hex / Pyramid / Sphere / Ramp**); **dock tool thumbnails** via shared off-DOM WebGL bakes; **teleporter** destination `<select>` in dock context + **Selected** pillar preview; compact build copy (no stacked hint paragraphs); **delete** control (`nq-cross` in tab row) for any selected placed object (**D** on desktop)
- [x] Client meshes: box / hex / pyramid / sphere / ramp, heights, hue-ring tint ([client/src/game/blockStyle.ts](../client/src/game/blockStyle.ts))
- [x] Active **claimable** (minable) blocks: gold styling + additive sparkle particles around the mesh ([client/src/game/Game.ts](../client/src/game/Game.ts) `makeMineableSparklePoints` / `updateMineableBlockSparkles`); **start claim** via **primary click on the block** (adjacent or pathfind-to-neighbor) or **right-click → Mine** on the unified world context menu. **Mine** menu row shows **`(50% ↑ time)`** in red (longer server hold for context-menu paths `world_ctx_*`). Inactive (cooldown) claimable blocks: primary click shows **“There's no NIM left here :(”** at the avatar (`showSelfPlayerActionMessage`). **Walk here** on an empty walkable tile (same menu, walk mode only; `Game.setWorldTileContextOpener` / `hud.showWorldTileContextMenu` in [client/src/main.ts](../client/src/main.ts)). **Mine** from the menu **pathfinds to a cardinal neighbor** when needed, then runs the same claim UI; **Walk here** with no valid path shows **“I can't move here”** as the same **floating world text** used for gate denial and mining rewards (`Game.showSelfPlayerActionMessage` → `showFloatingText`). **`beginBlockClaim.claimIntent`** (`world_ctx_adjacent`, `world_ctx_auto_walk`, `direct_adjacent_click`) is logged as **`begin_block_claim`** in gameplay JSONL for analytics. **Placing** claimable blocks: admins (or the authorized reward wallet) toggle the inline **Gold** switch next to the build-dock rotate controls ([client/src/ui/hud.ts](../client/src/ui/hud.ts) `buildDockClaimToggle`), which sends `claimable` on `placeObstacle`; the server gate is `canPlaceMineableBlocks` (`isAdmin` || `MINEABLE_BLOCK_PLACER_ALLOWLIST`). The same **Gold** toggle is **context-aware**: with a plain block **selected** it flips that block's gold/mineable state via `setObstacleProps` (sends explicit `claimable`; server initializes claim state when turning on, clears it when turning off, forces the block solid, and only authorized wallets may turn it on/off — unrelated prop edits preserve existing claim state).
- [x] **Object prefabs (designs)** — **Save** (drag 3×3–6×6 floor bbox → publish modal → `publishDesign` WS; **private** or **public** visibility) and **Place** (catalog, ghost + rotate, `placeDesignInRoom` WS; **overwrites** footprint) in any room where you can place objects (same `canEditRoomContent` / `allowPlaceBlocks` as blocks); JSON catalog ([server/src/designs.ts](../server/src/designs.ts), [server/src/designPlacement.ts](../server/src/designPlacement.ts)); build dock **Prefab** tab + **library** picker for dock favorites (**All** / **Mine** filter; right-click own prefabs to delete or toggle visibility; `deleteDesign` / `updateDesignVisibility` WS) ([client/src/ui/objectPrefabAuthoring.ts](../client/src/ui/objectPrefabAuthoring.ts), [client/src/ui/prefabDockPicker.ts](../client/src/ui/prefabDockPicker.ts), [client/src/game/Game.ts](../client/src/game/Game.ts))

## Billboards & world objects

- [x] **Signposts** (message signs on passable half-slabs) — server `signboards.ts`; client HUD tooltip + **floating `duotone-document` hint** (`nimiq-icons`) above tiles with `signboardId` in `obstacles` / `obstaclesDelta` ([client/src/game/Game.ts](../client/src/game/Game.ts)); subtle bounce; hidden in build mode; **hint hidden when stacked blocks sit on the tile or another placed mesh blocks the segment from the camera to the icon** (per-frame ray vs `blockMeshes`, skipping the signpost’s own group); **softer hint when idle, more solid while that signpost is tooltip-hovered**; uniform screen-space scale/vertical anchor across signpost blocks; sprite uses the same pick-skip pattern as name labels; **right-click / long-press** on a signpost tile adds **Read Sign** to the world context menu (profile-style modal with lighter scrim; author opens their profile; **owner** may **Edit message** via `updateSignboard`); hover tooltip identicon loads once per signboard (no re-fetch on every pointermove)
- [x] In-world **billboards** (slideshow URLs / presets, placement rules, live chart integration) — server `billboards.ts` + client `billboard*.ts`
- [x] **Mini-app discovery** — billboard `miniappTargetUrl` navigates to the competitor HTTPS URL at visit time (in Nimiq Pay, same WebView); `nimiqpay://miniapp` reserved for external share links — [client/src/net/miniappDeepLink.ts](../client/src/net/miniappDeepLink.ts)
- [x] **`/admin/campaign`** — **Pending approvals**, **Approved campaigns** (total funded / used / remaining; **unique viewers** / **link visits**; Live / Not live), **Expired / unfunded** (draft, pending payment, expired), and **Rotations** tabs; 3D preview with admin identicon on empty/placeholder states; **admin edit** project name + URL (`PATCH /api/admin/advertise/campaigns/:id`); **bonus credit** (`POST …/credit`) with ledger row and expired→approved reactivation ([server/src/adminCampaignPage.ts](../server/src/adminCampaignPage.ts))
- [x] **`/advertise` campaign portal** — wallet-gated dashboard with **New** / **Existing campaigns** tabs; create/edit draft campaigns (**Project Name**, **Project URL**, **Image**, **On-screen duration**); **device image upload** via `POST /api/advertise/campaigns/upload-image` → `/advertise/uploads/{uuid}.{ext}` (no wallet in path), or HTTPS image URL inline with **Choose file**; **3D billboard preview** when a campaign has an image (including pending approval); **on-screen duration** editable after funding; **Add funds** on approved/live campaigns (top-up prepaid balance without re-approval or going offline); **per-campaign transaction history** (date/time, NIM amount, [nimiq.watch](https://nimiq.watch) link) on Existing tab + admin preview panel; **audience stats** (unique viewers, total/avg on-screen time, link visits, last seen) from in-game proximity within **7 blocks** while tab-visible and not AFK; approved campaigns show **Live** / **Not Live** next to status (`inRotationSet` from rotation-set membership); public guide **`/advertise/how-it-works`** + [docs/advertise-guide.md](advertise-guide.md); APIs under `/api/advertise/*` with SQLite persistence ([server/src/advertisePage.ts](../server/src/advertisePage.ts), [server/src/advertiseGuidePage.ts](../server/src/advertiseGuidePage.ts), [server/src/campaignStore.ts](../server/src/campaignStore.ts), [server/src/campaignAnalyticsStore.ts](../server/src/campaignAnalyticsStore.ts), [server/src/campaignImageUpload.ts](../server/src/campaignImageUpload.ts))
- [x] **Teleporters** — cross-room (`placePendingTeleporter`, `configureTeleporter`, … in [server/src/rooms.ts](../server/src/rooms.ts)); **same-room linked pair** (`placeTeleporterBidirectionalPair`, reciprocal `teleporter.pairedPeerKey`, deleting either end removes both); **build dock** destination `<select>` when editing a placed teleporter ([client/src/ui/hud.ts](../client/src/ui/hud.ts), [client/src/main.ts](../client/src/main.ts))
- [x] **Gates** — `placePendingGate` (`exitDir`, optional **`colorRgb`** from the hue ring; `faceDir` ignored, **`rampDir` 0** for new gates; hinge animation follows **exit side** on the client), `openGate` (in **hub** / `lobby`, ACL not required to open or to use an open gate for movement); **`gateWalkBlocked`** to opener when the gate **visually opens** but exit/front is unwalkable or there is no path (client float: **You can't walk into that**); **green/red floor tint** on exit vs front neighbors while placing or moving a gate (walkability/blocking **feedback**; **placement** allows unwalkable exit/front; server still blocks hub spawn tiles, signpost on the gate tile, or other players on gate/exit/front (editor may stand on exit/front while rotating); obstacle `gate` wire includes `adminAddress` + `authorizedAddresses` (up to 5, legacy single-address migrated); **`setGateAuthorizedAddresses`** (gate owner or server admin; non-admins may only add wallets currently in the room); **gate ACL editor** in HUD (identicon rows, remove; **add opener** list shows identicon + name); **Permissions** button on the object panel opens the ACL editor; **context menu** lists **Open gate** for every viewer (unauthorized → client floating **You can't open that** with gentle spring motion); **double-click** the gate in walk mode when cardinally adjacent (within place radius) opens if allowed (non-hub: ACL; **hub**: anyone; same denial float if not); thin hinged client mesh (`colorRgb`); walk goal is the neighbor across the gate from the opener; **object panel** edits **opening direction** + **color** via `setObstacleProps` (`gateExitDir`; gate **`rampDir` 0** on save); while **moving** a gate, a **semi-transparent ghost** shows the preview at the hover tile, tints follow the preview, and the **placed** mesh at the source keeps its **original** opening until the move ends; **terrain/pathfind** does not treat a gate as a layer-1 “block top” stance (passage only), so when `gateOpen` ends the snap-to-grid step moves standers to nearby walkable floor instead of the door’s upper surface ([server/src/rooms.ts](../server/src/rooms.ts), [server/src/grid.ts](../server/src/grid.ts), [client/src/game/gateAuth.ts](../client/src/game/gateAuth.ts), [client/src/game/Game.ts](../client/src/game/Game.ts), [client/src/ui/hud.ts](../client/src/ui/hud.ts), [client/src/main.ts](../client/src/main.ts), [client/src/net/ws.ts](../client/src/net/ws.ts))
- [x] **Voxel text** labels in-world (`setVoxelText`) — **admin-only**; persisted via `voxelTexts` module

## Floor

- [x] `placeExtraFloor` / `removeExtraFloor` (rules: connectivity, not on core base removal incorrectly); **`colorRgb`** per tile from floor hue ring (persisted; legacy tiles default to extra-floor green); **left-click recolor** on existing core or extra floor (including under placed blocks); load merges **`world-state.json`** extra floor when split `data/rooms/*.json` omits tiles
- [x] Floor expand mode (`F`) on client
- [x] `POST /api/admin/random-layout` — random extra-tile growth (no auth in current server; see [server/src/index.ts](../server/src/index.ts))

## Replay / telemetry

- [x] Server JSONL event log per day (`EVENT_LOG_DIR`, `events-*.jsonl`) — session boundaries (with `nimiqPay` flag on `session_start`), moves, builds, chat
- [x] **End-of-day stats report to Telegram** (optional; UTC) — sent shortly after 00:00 UTC for the day that just ended: unique sign-ins, **new users**, **Nimiq Pay vs other**, NIM paid out, **NIM still pending in the payout queue plus a combined "Total NIM (sent + pending)"** (so all NIM earned that day is counted, snapshotted before the end-of-day flush), and **active in-game time** (capped-gap estimate, excludes long AFK). Toggle `DAILY_STATS_TELEGRAM_ENABLED` (default on when Telegram configured), optional `DAILY_STATS_TELEGRAM_CHAT_ID`, `DAILY_STATS_LOOKBACK_DAYS` (default 400 for new-user detection). Manual run: `POST /api/admin/daily-stats/send?day=YYYY-MM-DD&send=1` (analytics admin; reports pending but does not flush). **`/admin/system`** has a **Send last 24h to Telegram** button (rolling 24h; `POST /api/admin/system/daily-stats/send`, `preview=1` to preview; system admin). When the **World Cup** feature is on, a **second message** follows the stats summary: the day's **goal recap** — total goals scored, top-3 teams (countries) on a 🥇🥈🥉 podium, the day's **MVP** (top scorer), and the leading scorers (built from the per-day score store; skipped on days with no goals) ([server/src/worldcup/goalDayReport.ts](../server/src/worldcup/goalDayReport.ts), [server/src/worldcup/scoreStore.ts](../server/src/worldcup/scoreStore.ts)) ([server/src/dailyStatsReport.ts](../server/src/dailyStatsReport.ts), [server/src/eventLog.ts](../server/src/eventLog.ts), [server/src/adminSystemPage.ts](../server/src/adminSystemPage.ts))
- [x] **End-of-day NIM payout auto-flush** (UTC) — at the end of each UTC day, **after the stats report is sent**, every recipient's pending payout jobs are combined into one on-chain transfer (same path as admin **Payout in full**) and the queue is cleared; failed sends stay pending for the Payout Service worker to retry. Toggle `NIM_PAYOUT_DAILY_FLUSH_ENABLED` (default on when `PAYOUT_SERVICE_*` is configured); runs from the daily-stats scheduler even when the Telegram report is disabled ([payout-service/src/queue.ts](../payout-service/src/queue.ts), [server/src/dailyStatsReport.ts](../server/src/dailyStatsReport.ts))
- [x] **Payout Service sidecar** — outgoing NIM runs in dedicated `payout` container (default in compose); game server enqueues via Outbox ([payout-service/](../payout-service/), [server/src/payoutGateway.ts](../server/src/payoutGateway.ts); [payout-cutover-runbook.md](payout-cutover-runbook.md))
- [x] `GET /api/replay/players`, `/api/replay/sessions`, `/api/replay/session/:id/events` (Bearer JWT)
- [x] Main menu “Session replay” — pick player, session, action timeline (**localhost only**; hidden on public origins)
- [x] Server-side max distance for block actions (`PLACE_RADIUS_BLOCKS`, default 9 world units on XZ, circular); floor recolor uses the same limit as an **axis-aligned square** (±R tiles from the player's tile on X and Z)
- [x] **Latency diagnostics** — debug stats panel (click your own identicon in your profile) shows a **server round-trip-time graph** (1 Hz `clientPing`/`clientPong` sparkline; min/avg/max + red spike dots); server logs **`[event-loop]`** stall lines (`EVENT_LOOP_STALL_LOG_MS`, default 50) — after payout cutover these should not correlate with on-chain sends ([client/src/ui/hud.ts](../client/src/ui/hud.ts), [client/src/main.ts](../client/src/main.ts), [server/src/adminSystemMonitor.ts](../server/src/adminSystemMonitor.ts); see [docs/nim-payout-tracing.md](nim-payout-tracing.md))

## Feedback (player tickets)

- [x] **In-game feedback v2** — **Feedback** on **your profile** card (footer, bottom-left; red dot when an admin reply is unread): overlay with **New** (kind: bug / feature / suggestion) + **My feedback** list with threaded admin replies and player follow-ups; optional **+NIM** chip when a ticket was rewarded ([`client/src/ui/hud.ts`](../client/src/ui/hud.ts), [`client/src/main.ts`](../client/src/main.ts))
- [x] **Chat report** — right-click chat line → **Report Message**: reported player/message shown read-only; player submits only their reason; server stores structured `reportContext` + snapshot of the reported user's recent chat backlog (~10 min)
- [x] **Player APIs** — `POST /api/feedback` (create ticket + first message; optional `kind`, `source: "report"` with `report` object for chat reports), `GET /api/feedback/mine` (`unreadCount` + per-ticket `unread`), `GET /api/feedback/:id` (marks read), `POST /api/feedback/:id/messages` (JWT; 700 chars; 20s send cooldown; max **3 new tickets / wallet / day**)
- [x] **Persistence** — JSON store default `server/data/feedback/tickets.json`; optional **`FEEDBACK_STORE_FILE`**
- [x] **Admin hub** — `/admin/feedback` + `GET/PATCH /api/admin/feedback`, reply + status (`open` / `answered` / `integrated` / `closed`), **`POST …/reward`** (0.10–2 NIM via `enqueueNimPayout` when status is **integrated**; one reward per ticket); chat reports show reported player, quoted message, and captured chat history ([`server/src/feedbackTicketStore.ts`](../server/src/feedbackTicketStore.ts), [`server/src/adminFeedbackPage.ts`](../server/src/adminFeedbackPage.ts))
- [x] **Telegram ping** (optional) — new tickets, **username set** (`Name Update: wallet -> name`), connect notices, and the **end-of-day stats report** via `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` when configured; store is source of truth
- [x] **Admin Rooms manager** — `/admin/rooms` (system-admin only): **Official rooms** (built-ins + admin official) and **Player Owned** tabs; per-room **cards** with a top-down **2D thumbnail** (`GET /api/admin/rooms/:id/thumbnail.png`, [server/src/roomThumbnailImage.ts](../server/src/roomThumbnailImage.ts); Pixel reuses `/pixels.png`) and an **interactive 3D preview** modal (standalone `/roomPreview.html` reusing the Three.js `Game` fed by `GET /api/admin/rooms/:id/layout`; no WS join, no avatar); inline **property editing** (name, public/hidden, background hue/neutral) and a per-room **builder allowlist** (wallets allowed to build/edit beyond owner/admin) via `PUT /api/admin/rooms/:id`. Builder list persists in `rooms.json` **v7** and extends `canEditRoomContent()` ([server/src/adminRoomsPage.ts](../server/src/adminRoomsPage.ts), [server/src/index.ts](../server/src/index.ts), [server/src/roomRegistry.ts](../server/src/roomRegistry.ts), [server/src/rooms.ts](../server/src/rooms.ts), [client/src/roomPreview.ts](../client/src/roomPreview.ts))

## UI / shell

- [x] **Patch notes** — main-menu version label links to **`/patchnotes`**, which shows frozen semver releases from `patchnote/versions/<x.y.z>/public/*.md` (bundled at client build time); **version** and **audience** dropdowns pick release and tier (**Brief** / **Players** / **Operators** / **Developers** / optional **Hotfix** when `04-hotfix.md` exists), with newest release selected by default; optional list tags **`[NEW]`**, **`[FIX]`**, **`[CHANGE]`**, **`[PERF]`**, **`[OPS]`**, **`[SEC]`** render as badges ([`client/src/patchnotes/`](../client/src/patchnotes/), editorial guide [patchnotes-release.md](patchnotes-release.md))
- [x] In-game **header marquee** — streak ticker: seamless duplicated strip (`translateX` `0` → `-50%` of track), duration from measured half-width; **per-chunk invisible fill** when text is shorter than the ticker so a full loop covers the **visible width** on large screens; `ResizeObserver` + image load/error remeasure (identicons); streak names open that player’s profile card. With announcements, next line after **one full horizontal loop** (`animationiteration`); server **`marqueeStreakSeconds`** is safety fallback; message dwell **`marqueeMessageSeconds`** (`GET /api/header-marquee`, `/admin/header`, persisted JSON stores). **Client:** after the player has seen every announcement line once (streak+news or news-only), the marquee **fades out** and stays hidden for **10 minutes** unless **`/admin/header`** lines change (signature match in `localStorage` key `nspace.headerMarquee.newsSuppress`); **streak-only** (no lines) is unchanged
- [x] In-game **player profiles** — profile card shows display name, wallet actions, description, recent aliases, Nimiq Pay session badge, and up to 3 owned rooms from `GET /api/player-profile/:address` (public rooms for everyone; private rooms only to the owner or admins). Server and client treat visibility as **`isPublic === true`** for non-owner, non-admin viewers so private or ambiguous entries never appear on someone else’s card. Each `rooms[]` entry includes a live **`playerCount`**; the server returns the three busiest rooms first (by real players online). Rows show **room name + live count** with the Nimiq **`person-1`** icon (`i-nimiq:person-1`, inline SVG via `nimiq-icons`) instead of join codes. Room rows open a join confirmation; admin-only controls use inline username edit + Actions dropdown for clear / ban name / mute / **ban mining** (optional admin note). Restricted wallets see `miningBanned` on their own profile. The HUD **players** pill shows **in this room / online total** when those numbers differ.
- [x] **Mining Restriction (admin)** — game admins can block a wallet from claimable-block NIM mining via profile Actions or **`/admin/moderation`**; stored in `moderation.json` (`miningBanned`, optional `note`); enforced at `beginBlockClaim` and `completeBlockClaim`. **Guest** sessions cannot earn NIM from block claims (wallet required).
- [x] **Action Wheel** — right-click (desktop) / long-press (touch) your **own avatar** opens a **hexagonal** menu centred on you; the transparent hexagonal **Hub** in the middle frames your avatar. The hexagon has **six fixed Sectors** (one per edge): a bottom **Nav Sector** is **Close** (root) / **Back** (sub-wheel), the rest hold actions, and unused edges are dim, non-interactive **reserved** Sectors so the hexagon always reads whole; root Sectors now carry **text labels**. Root offers **Emoji**, **Home**, **Games**, and **1v1** (a multi-level tree with breadcrumb Back). **Emoji** opens a paged sub-wheel (Back + 4 emotes + a wrap-around **More** Sector) that sends the emote to chat. **Home** → **My Rooms** (opens the Rooms browser) / **Private Room** (open or return to your Play Space + Share Panel). **Games** → **Soccer** → **Free Play** (join the Free Play Field) or **1v1** → **This room** (Open to 1v1 / Cancel — raises or clears your open Challenge, disabled on a pitch/Match) / **Invite** (open your Play Space). **1v1** is a root shortcut straight to a game's 1v1 leaf (Soccer → This room / Invite). **Guests** see a trimmed wheel (Emoji + 1v1 only — Home/Free Play hidden, since they're confined to their Play Space). Persistent (no auto-close); dismiss via Close, outside-click, **Esc**, or **walking**. Drawn as inline SVG polygon Sectors so each Sector's shape is its hit target; pure geometry/paging in [`client/src/ui/actionWheelGeometry.ts`](../client/src/ui/actionWheelGeometry.ts) (unit-tested), component + wiring in [`client/src/ui/hud.ts`](../client/src/ui/hud.ts) / [`client/src/main.ts`](../client/src/main.ts). Replaces the old flat quick-emoji strip (see [CONTEXT.md](../CONTEXT.md), [docs/adr/0001-action-wheel-replaces-emoji-strip.md](adr/0001-action-wheel-replaces-emoji-strip.md))
- [x] **Flags render as Twemoji images** — country flags are drawn from self-hosted Twemoji SVGs (`client/public/flags/<cc>.svg`, fetched by [`client/scripts/fetch-flag-svgs.mjs`](../client/scripts/fetch-flag-svgs.mjs)) instead of Unicode regional-indicator emoji, because **Windows ships no flag glyphs** so Chromium/Brave show them as the two-letter code (e.g. "AT"). Centralized in [`client/src/ui/flags.ts`](../client/src/ui/flags.ts) (`createFlagImg` / `appendTextWithFlags` for DOM, `loadFlagImage` for canvas) and used by the scoreboard, match HUD/countdown, country picker, profile chip, **chat log + 3D chat bubbles** (the Flag Emote), the **Emote Wheel** glyph (SVG `<image>`), the 3D **crowd banners** (`fieldVisuals.ts`), and the spectate **portal billboard**. `.flag-emoji` CSS sizes them in `em`. Same-origin assets keep the crowd-texture canvases untainted; missing/failed loads fall back to the alpha-2 `alt` text
- [x] **Profile flag + Flag Emote** — the player's own profile card shows a **country flag chip** beside the name; clicking it opens the shared country picker ([`client/src/worldcup/countryPickerModal.ts`](../client/src/worldcup/countryPickerModal.ts)). The chosen country is the **single per-player country reused from the World Cup** (stored in `worldcup-scores.json`), so picking it here also credits World Cup goals; it is accepted **regardless of `WORLDCUP_ENABLED`** (the season gate only hides the seasonal scoreboard/crowd extras). The flag also appears read-only on **other** players' cards via `country` on `GET /api/player-profile/:address`. When a country is set, that flag is **prepended as the first/top Emote** on page one of the player's own Emote Wheel and broadcasts as a chat bubble like any other emote. Self country flows in **every room** via `welcome.worldcupSelfCountry`; client tracks it globally and feeds `hud.setSelfCountry()` ([`client/src/ui/hud.ts`](../client/src/ui/hud.ts), [`client/src/main.ts`](../client/src/main.ts), see [CONTEXT.md](../CONTEXT.md) **Flag Emote**)
- [x] Letterboxed 16∶9 HUD, status line, chat log + input
- [x] **Player Menu** — persistent bottom-right identicon button opens a vertical navigation popover (Profile, Wardrobe, Rooms, Return to Hub, Logout; guest variant with Get a Wallet + Leave). Desktop shortcuts toggle: `C` profile, `Y` achievements, `O` rooms (wallet sessions only for `Y`/`O`). Reuses existing profile/wardrobe/rooms surfaces; hides during build dock and stream cinema ([client/src/ui/playerMenu.ts](../client/src/ui/playerMenu.ts), [client/src/ui/hud.ts](../client/src/ui/hud.ts), [client/src/main.ts](../client/src/main.ts))
- [x] **Nimiq Pay portrait-first HUD** — Pay WebView sets `nspace-nimiq-pay-portrait` / `nspace-nimiq-pay-landscape` from viewport aspect; **portrait** fills the Pay viewport (letterbox = frame; orthographic camera adapts), **spread build dock** (bottom carousel + action bar; floating properties left, color/preview top-right), one-row top strip (brand + wallet + player/NIM/lobby), top-right **Return to Hub** + **Rooms** + **Build** stack; **landscape** keeps height-first letterbox + compact right action rail (best-effort) ([client/src/ui/pseudoFullscreen.ts](../client/src/ui/pseudoFullscreen.ts), [client/src/ui/hud.ts](../client/src/ui/hud.ts), [client/src/style.css](../client/src/style.css))
- [x] Fullscreen toggle
- [x] Input shell to reduce accidental browser shortcuts (best-effort)
- [x] Admin overlay (when `VITE_ADMIN_ENABLED=true`): random layout, fog tuning, zoom limits, identicon / voxel tools, **build HUD inspector preview** per-profile scale + pan (world X / Z; `localStorage` `nspace_inspector_preview_layouts_v2` JSON; legacy single-key pan/scale maps to **default** profile)

## NPCs (fake players)

- [x] `FAKE_PLAYER_COUNT` env — server-side wanderers merged into `PlayerState` snapshots (disabled in **Pixel**)
- [x] Display names chosen from a curated list ([server/src/guestNames.ts](../server/src/guestNames.ts)); duplicates get a numeric suffix
- [x] Idle up to 2s between choosing new random destinations; cleared when room has no real clients

## World Cup soccer (seasonal, feature-flagged)

Gated behind **`WORLDCUP_ENABLED`** (server) / **`VITE_WORLDCUP_ENABLED`** (client build), both default **on**. Isolated under [server/src/worldcup/](../server/src/worldcup/), [client/src/worldcup/](../client/src/worldcup/), [worldcup/issues/](../worldcup/issues/). Off ⇒ no hub→field door, ball, goals, scoring, or UI. Deprecation/removal recipe in [docs/THE-LARGER-SYSTEM.md](THE-LARGER-SYSTEM.md) ("Server-simulated dynamic objects") + `docs/reasons/reason_615240.md`.

- [x] **Server-simulated kickable ball** — first dynamic non-player object; ground-only 2D physics (position/velocity/friction/wall+obstacle bounce) on the existing 20 Hz room tick; **proximity kick** (run into it, no input); throttled `ballState` broadcast, client-side interpolation ([server/src/worldcup/ballPhysics.ts](../server/src/worldcup/ballPhysics.ts), [ballStore.ts](../server/src/worldcup/ballStore.ts), [ballTick.ts](../server/src/worldcup/ballTick.ts), [client/src/game/Game.ts](../client/src/game/Game.ts))
- [x] **Free Play Field room** (`field`) — standalone built-in room behind the flag; hub↔field door pair (client/server parity); two goals (`west`/`east`, no teams — any goal credits the last kicker within `LAST_KICKER_WINDOW_MS`); a goal counts only when the ball **crosses the line through the mouth** (end-line wall opens at the mouth + net box contains it); block/floor edits disabled on the pitch
- [x] **Pitch visuals** (client-only, [client/src/worldcup/fieldVisuals.ts](../client/src/worldcup/fieldVisuals.ts)) — procedural soccer-ball texture, grass + line-markings surface overlay, goal netting, tiered stadium stands; built/disposed by `syncWorldcupGoals`, `raycast`-disabled so they never affect floor picking or collision
- [x] **Cheering crowd** (client-only, `WorldcupCrowd` in [fieldVisuals.ts](../client/src/worldcup/fieldVisuals.ts)) — Nimiq-identicon sprite spectators (shared async-loaded pool) on the stands with idle bob, ambient synchronized wave cheers, emoji bubbles, and a full eruption on every goal (`goalScored` → `game.worldcupCrowdCheer()`); server fake-player wanderers disabled on the pitch (`roomAllowsFakePlayers` excludes the field room) so the play area stays clear
- [x] **Daily (UTC) scoreboard + history** ([scoreStore.ts](../server/src/worldcup/scoreStore.ts)) — goals bucket per UTC day and reset at 00:00 UTC; completed days archived to `history` (retained), previous day's winner kept as `prevWinner`; country *choices* persist across resets; forward-only `rolloverIfNeeded` on the room tick broadcasts the reset; legacy cumulative files migrate to a `0000-legacy` entry; `GET /api/worldcup/leaderboard` exposes `day`/`previousWinner`/`history`
- [x] **Champion crowd flag** — the stadium crowd waves the previous UTC day's winning country's flag (held-up banners + flag-biased cheer emoji via `WorldcupCrowd.setFlag()`); scoreboard shows a "Yesterday's champion" line and a "resets 00:00 UTC" note; flag flows via `welcome.worldcupPrevWinnerCountry` + `worldcupLeaderboard.prevWinnerCountry`
- [x] **Free (any-direction) movement on the pitch** — field-room `moveTo` walks a straight line to the exact clicked float point (no `snapToTile`/`pathfindTerrain`, drift-snap skipped) so the ball can be kicked at any angle; client sends the raw float floor-hit + straight preview line; doors still use the snapped standing-tile check ([server/src/rooms.ts](../server/src/rooms.ts), [client/src/game/Game.ts](../client/src/game/Game.ts))
- [x] **Country-tagged scoring** — pick once per wallet from a searchable ISO 3166-1 alpha-2 flag list (auto-opens on first uncredited goal; changeable anytime); per-country tally + per-player goal counts; **immutable per-goal attribution**; pending attribution for goals scored before a country is chosen; **only the official field room counts** ([server/src/worldcup/scoreStore.ts](../server/src/worldcup/scoreStore.ts), [client/src/worldcup/countryPickerModal.ts](../client/src/worldcup/countryPickerModal.ts))
- [x] **Live field scoreboard HUD** (top countries, updates on `goalScored`) + flag button to re-open the picker ([client/src/worldcup/scoreboard.ts](../client/src/worldcup/scoreboard.ts)); **`GET /api/worldcup/leaderboard`** (404 when flag off). Mounted **inside the `.letterbox` game area** (absolute, responsive width) so it never spills into the black letterbox bars, and **tap the header to collapse/expand** — collapsed shows a compact `⚽ {leader flag} {goals}` pill; the choice persists in `localStorage` (`wc_scoreboard_collapsed`). Its top is **anchored below the measured top chrome** (`var(--hud-below-top-wrap)` + the top-right **Return to Hub** button) so the panel never covers the header/login-streak marquee or the Return to Hub control
- [x] **Personal goal-reward note (Free Play Field)** — the scorer (and only the scorer) sees a small note under the GOAL banner confirming the **NIM earned** (`+{amount} NIM earned!`) or, when no NIM was paid, that they **hit their daily cap** / the day's pool is spent (`scoreboard.flashReward`). Sent as a targeted `goalRewardOutcome` WS message (never broadcast — the cap/budget is personal, not public) ([server/src/rooms.ts](../server/src/rooms.ts), [client/src/worldcup/scoreboard.ts](../client/src/worldcup/scoreboard.ts), [client/src/main.ts](../client/src/main.ts))
- [x] **Place a ball anywhere** — build-dock **Ball** prop (Props tab) places a kickaround ball in buildable rooms (capped at `MAX_PLACED_BALLS_PER_ROOM`); placed-ball defs persisted in `server/data/worldcup-balls.json` (live positions reset on restart); `placeBall` / `removeBall` WS intents
- [x] **Goalies** — a server-controlled keeper defends every goal in the **field and every Match Pitch**; pure lateral ball-tracking with reaction lag + capped move-speed ([server/src/worldcup/goalie.ts](../server/src/worldcup/goalie.ts)), stepped on the room tick and broadcast as throttled `goalieState` positions; rendered client-side as a fixed **House Keeper** identicon billboard + keeper ring (one stable seed for every goal, never mistakable for a player) ([client/src/game/Game.ts](../client/src/game/Game.ts)); A/B `WORLDCUP_GOALIE_MODE` — `kicker` (injected into the proximity-kick path, clears the ball) or `blocker` (circular collider inside `stepBall`); a goal that deflects off the Goalie credits the **last real (human) kicker** (only a goal with no recent human touch credits nobody). **Tuned to be beatable**: the keeper patrols only the inner part of the mouth (`WORLDCUP_GOALIE_COVERAGE_PCT`, default 60% → a permanent corner gap on each side), clears the ball within a **shorter kicker-mode reach** than a player (`WORLDCUP_GOALIE_KICK_REACH_TENTHS`, default 0.3 vs player 0.6), and is slower + laggier (`WORLDCUP_GOALIE_MOVE_SPEED` 2.5, `WORLDCUP_GOALIE_REACTION_MS` 260), so a well-placed corner/pace shot scores while a tame shot down the middle is saved
- [x] **Goal rewards (Free Play Field only)** — scoring queues a random NIM payout (default 0.5–2 NIM per goal) to the credited **last real (human) kicker** — which survives a Goalie deflection / own-goal — via the existing `enqueueNimPayout` queue; pure evaluator + per-UTC-day store ([server/src/worldcup/goalReward.ts](../server/src/worldcup/goalReward.ts)): deterministic idempotent `claimId`, **Solo Goal** half-rate when only one player is present, full rate when **Contested** (≥2 players); per-wallet daily cap and global daily budget are **unlimited by default** (env knobs `WORLDCUP_GOAL_REWARD_DAILY_CAP_PER_WALLET` / `_DAILY_BUDGET_NIM` = 0, set positive values only as an emergency brake); blocked goals still count for the leaderboard, only the payout stops; 1v1 Matches never pay (see [worldcup/adr/0002](../worldcup/adr/0002-nim-rewards-free-play-only.md), [docs/reasons/reason_562094.md](reasons/reason_562094.md), [docs/reasons/reason_391826.md](reasons/reason_391826.md)). The on-chain transfer message reads **"Scored a goal on Nimiq Space!"**; the scorer is told the outcome in-game via the targeted `goalRewardOutcome` message (earned NIM / cap reached)
- [x] **1v1 Match loop** (server) — pure reducer: regulation timer → Golden Goal sudden-death (capped, then Draw) → terminal Win/Draw/Opponent-left outcome ([server/src/worldcup/match.ts](../server/src/worldcup/match.ts)); now driven end-to-end in [rooms.ts](../server/src/rooms.ts): a player raises an open **Challenge** (per-conn flag, auto-cleared after `WORLDCUP_CHALLENGE_TIMEOUT_MS`, surfaced on broadcast player state as a floating bubble); another player **accepts** from the other-player context menu (same-room, first-accept-wins); the server spins up an ephemeral **Match Pitch** (field template, never persisted — [matchPitch.ts](../server/src/worldcup/matchPitch.ts), [ADR 0001](../worldcup/adr/0001-ephemeral-match-pitches.md)), snapshots both origins, teleports both in, runs the reducer broadcasting `matchState`/`matchEnded`, and after `WORLDCUP_MATCH_RESULT_LINGER_MS` returns both home and tears the empty room down; disconnect/leave forfeits to the opponent; Match goals score by which net is breached (own goals count for the opponent) and **never pay NIM**. **Navigating to another room mid-Match** (e.g. Action Wheel → Join Free Play Field) **forfeits to the opponent and then loads the requested room** — `joinRoom` no longer silently drops while `matchId` is set (Match Pitches themselves stay non-joinable). An open **Challenge is room-scoped**: changing rooms clears it (in `teleportPlayer`) so a stale "Accept 1v1" never follows the player into another room. The "⚽ Accept 1v1 challenge" context-menu item is gated on the target's `challengeOpen`; its visibility relies on the `hidden` attribute, which needs an explicit `.other-player-ctx__item[hidden] { display:none !important }` rule in [client/src/style.css](../client/src/style.css) because the item's author `display:block` otherwise beats the UA `[hidden]` default (the item was previously always shown). As defence-in-depth the client also derives `challengeOpen`/`chatTyping`/`nimSendAway`/`worldcupCountry` from each `stateDelta` snapshot (never inherited from the prior tick) so a cleared flag can't leak forward
- [x] **Play Space (invite-only multiplayer room)** — Action Wheel → **Home → Private Room** (or **Games → Soccer → 1v1 → Invite**) opens a private, ephemeral **Play Space** with a shareable `nimiq.space/join/{slug}` link + QR. Multiple people can join — the wallet **creator** plus up to `DIRECT_INVITE_MAX_OCCUPANTS` (default 8 total) **Guests** — and **anyone inside** can raise their own 1v1 Challenges (the normal Challenge → Kickoff Countdown → ephemeral Match Pitch flow); Matches **return players to the Play Space** and surface its **Spectate Portal** there. Guests open the link at the **Join gate** (`/join/{slug}`): peek-before-claim, saved-wallet account list (or sign-in vs guest fork), optional wallet fast-path (`POST /api/invite/join-wallet`), then guest identicon + nickname + Enter game — and land **directly in the Play Space** (the WS connect now honors an explicit invite-lobby room instead of the `resume=1` → chamber fallback). **Guests are confined** to their Play Space and the Match Pitches it launches (`joinRoom`/`enterPortal`/arbitrary-room connect are blocked), the **creator comes and goes freely**, and the room is torn down only when no member remains (it survives while members are away in a Match). **Guests never earn NIM** (`evaluateGoalReward` excludes `guest:*`). Multi-participant lifecycle reducer/store in [server/src/directInvite/](../server/src/directInvite/) (phases `open`/`closed`/`expired`; [ADR 0003](../worldcup/adr/0003-guest-sessions-direct-invite.md), [worldcup/PRD-direct-invite.md](../worldcup/PRD-direct-invite.md)); **layout from Play Space Templates** ([server/src/playSpaceTemplate/](../server/src/playSpaceTemplate/), [ADR 0004](../worldcup/adr/0004-play-space-templates.md)) — admin-managed **Build Shell** snapshots seed each new space (default lounge bootstrapped from legacy [playSpaceLayout.ts](../server/src/directInvite/playSpaceLayout.ts)); **ephemeral session edits** let all occupants build/recolor within bounds for the session (nothing persists; teleporters/gates forbidden). System admins may pick a non-default template at create and manage templates on `/admin/rooms` → **Play Space templates**. HTTP `POST /api/invite/create`, `GET /api/invite/peek/:slug`, `GET /api/invite/redeem/:slug`, `POST /api/invite/join-wallet/:slug`, `POST /api/invite/nickname`, `POST /api/invite/upgrade`; WS `directInviteState` (roster + `occupancy`/`capacity`) / `directInviteError` + `cancelDirectInvite` (leave); client **Join gate** + share panel in [client/src/invite/](../client/src/invite/)
- [x] **Play Space Share Panel + persistent Share Button** — the room code (`{slug}`), copyable join link, and QR now live in a **dismissible** card (close ✕) that **auto-opens once** when the **creator** (or anyone who did not arrive via `/join/{slug}`) enters the space, instead of an always-blocking overlay. **Guests who open an invite link** land in the Play Space without the panel popping up (they already used the link); the **🔗 Room code** toolbar button is still available to re-open it. Latest `directInviteState` is cached client-side so the button can re-render the current code/QR; roster updates only refresh the panel when it's already open. Components in [client/src/invite/lobbyOverlay.ts](../client/src/invite/lobbyOverlay.ts) (`isOpen`, `onClose`, room-code display) + HUD `setPlaySpaceShareVisible`/`onPlaySpaceShareOpen` in [client/src/ui/hud.ts](../client/src/ui/hud.ts), wired in [client/src/main.ts](../client/src/main.ts)
- [x] **Kickoff Countdown** — accepting a Challenge no longer teleports instantly: both players show a 🤝 handshake bubble and a "Match starting in N…" overlay in the origin room (`WORLDCUP_MATCH_COUNTDOWN_MS`, default **10s**) before the server teleports them onto the Match Pitch; when the countdown hits zero a **"Entering the match…" loading screen** covers the teleport (the pitch welcome hides it; cancelled by an earlier welcome/disconnect); a disconnect/leave during the countdown aborts it ([server/src/rooms.ts](../server/src/rooms.ts) `worldcupBeginMatch`/`worldcupTickPending`, [client/src/worldcup/matchCountdown.ts](../client/src/worldcup/matchCountdown.ts), [client/src/main.ts](../client/src/main.ts) `worldcupMatchEnterTimer`)
- [x] **Flat scoreboard bar** — the Match HUD is a horizontal bar pinned directly under the top HUD chrome: `{flag}{identicon} scoreA · clock · scoreB {identicon}{flag}` with the local side highlighted, and a Leave button at the right edge during live play ([client/src/worldcup/matchHud.ts](../client/src/worldcup/matchHud.ts)); when a Match ends a centre-screen **Match Result Overlay** shows both identicons (winner full opacity, loser faded), personalized headline, final score, `Returning in N…` countdown, and Leave / Stop watching (default linger **10s**, `WORLDCUP_MATCH_RESULT_LINGER_MS`); movement frozen until return home; player countries ride along on `matchState`/`matchEnded` (`aCountry`/`bCountry`); the bar is mounted **inside the `.letterbox` game area** so it spans the rendered game width rather than the full viewport
- [x] **Crowd Allegiance** — the stands no longer wave only the champion flag: on a Match Pitch they split by side (west half waves side a's flag, east half side b's); on the Free Play Field they reflect the live roster — the distinct countries of players currently present (`worldcupCountry` on player snapshots), falling back to the champion flag when nobody has picked ([client/src/worldcup/fieldVisuals.ts](../client/src/worldcup/fieldVisuals.ts) `WorldcupCrowd.setSideFlags`/`setRosterFlags`)
- [x] **Spectating** — a "{identicon} vs {identicon}" **Spectate Portal** spawns in the room a Match was started from; **walk onto the portal's footprint tile** (door-style proximity, `Game.getStandingSpectatePortal`) to raise a **"Watch"** intent pill, then press it to drop into the Match Pitch stands as a **Spectator** (fixed seat, excluded from ball physics, can't move) up to `WORLDCUP_MATCH_SPECTATOR_CAP` (pill reads "Stands full" past the cap) — no more teleport-on-click, a click on the portal just walks you toward it; a **loading screen** covers the drop into the stands. Spectators are returned home when the Match ends (or via Leave, no forfeit) and the portal is removed on end ([server/src/rooms.ts](../server/src/rooms.ts) `requestSpectate`/`worldcupBroadcastPortal`, [client/src/game/Game.ts](../client/src/game/Game.ts) portal markers + standing-tile detection, [client/src/main.ts](../client/src/main.ts) Watch pill + loading); implements [worldcup/issues/80-spectating.md](../worldcup/issues/80-spectating.md)
- [x] **Match Pitch view polish** (client-only, no server/WS changes) — Spectators are seated on the stand **front row** instead of inside the seating (`frontRowStandSeat` in [client/src/worldcup/fieldVisuals.ts](../client/src/worldcup/fieldVisuals.ts), applied after the per-frame avatar lerp); while spectating the camera **frames the whole pitch at a locked zoom** (centred on the pitch, not the stand seat); each Match player sees a bobbing cyan **attacking-goal arrow** over the net they attack (side a = east, side b = west — [client/src/worldcup/goalArrow.ts](../client/src/worldcup/goalArrow.ts)); and **side a's camera defaults to 180°** on entry (west/NW spawn) so both players shoot the same way down-screen, still freely re-orbitable. Driven from the `matchState` handler in [client/src/main.ts](../client/src/main.ts) via `Game.setWorldcupMatchParticipants` / `setWorldcupSpectatorView` / `setWorldcupAttackGoal` / `setWorldcupMatchOrientation`; torn down on leaving the pitch
- [x] **Movement Mode Toggle** — experimental top-left control on touch / Nimiq Pay hosts while on the Free Play Field or a Match Pitch (active players only): switches **Pitch Movement Mode** between **Tap** (stationary tap walks; drag does not summon the stick) and **Joystick** (drag summons the floating Touch Joystick; taps do not walk). Shows the active label (`Tap` / `Stick`); choice persists in `localStorage`; switching stops any in-progress walk or stick immediately; stacks below the Match HUD bar when that bar is visible ([client/src/worldcup/movementModeToggle.ts](../client/src/worldcup/movementModeToggle.ts), [client/src/game/Game.ts](../client/src/game/Game.ts) `setWorldcupPitchMovementMode`)
- [x] **Touch Joystick** — on touch devices / the Nimiq Pay mini-app, a **floating** left-thumb virtual stick steers the player continuously on the pitch in **Joystick** Pitch Movement Mode: it materializes wherever the thumb presses down and drags (no fixed home), emitting throttled `moveTo` toward a far clamped point via the camera-relative ground basis; stops on release. A single-finger drag past ~14px becomes the stick; the steering finger keeps priority (two-finger camera/zoom is ignored while it owns a finger). Release sends a dedicated un-rate-limited stop (`moveTo {stop:true}`) so the player halts at once rather than gliding toward the last far target. `Game.ts` owns the tap-vs-drag gesture on its pitch pointer pipeline; the joystick file is a visual-only widget. Soccer rooms only ([client/src/worldcup/joystick.ts](../client/src/worldcup/joystick.ts), [client/src/game/Game.ts](../client/src/game/Game.ts) `beginWorldcupStick`/`worldcupJoystickMove`)
- [x] **Ball Edge Marker** — when the ball is off the letterboxed game viewport, active players on the Free Play Field or a Match Pitch see a screen-edge chevron pointing toward it; opacity scales with how far past the edge the ball sits and the marker hides the moment any part of the ball enters frame (never shown to Spectators; stays visible through kickoff freezes and goal banners). Pure geometry in [client/src/worldcup/ballEdgeMarkerGeometry.ts](../client/src/worldcup/ballEdgeMarkerGeometry.ts), HUD in [ballEdgeMarker.ts](../client/src/worldcup/ballEdgeMarker.ts), driven from the main loop via `Game.getPrimaryWorldcupBallScreenPosition()` ([client/src/main.ts](../client/src/main.ts))
- [x] **Click-toward-tap** — in grid rooms an off-tile / non-walkable click now walks to the nearest walkable tile under the cursor (expanding-ring search) instead of doing nothing ([client/src/game/Game.ts](../client/src/game/Game.ts) `nearestWalkableTileToWorld`)
- [x] **Outfield Margin** — players may step ~1 tile past the pitch edges (`WORLDCUP_FIELD_OUTFIELD_MARGIN`, default 1.0) so they can get fully behind a ball pinned against a wall; only player movement is widened, the ball's collision walls stay at the true bounds ([server/src/rooms.ts](../server/src/rooms.ts) `worldcupMoveClampBounds`, [client/src/game/Game.ts](../client/src/game/Game.ts) `worldcupFieldMoveBounds`); **tap/click-to-move now clamps to the outfield wall (bounds + margin), not the touchline** — a tap toward / onto the stands walks you all the way to the wall in line with where you tapped, so you can get fully behind a wall-pinned ball by mouse/tap (not just the joystick); the path preview matches ([client/src/game/Game.ts](../client/src/game/Game.ts) `tryFieldFreeWalkAt`, `previewWalkNavigationAt`)
- [x] **Goal Banner + Kickoff Reset** (1v1) — a goal in a Match Pitch now broadcasts `matchGoal`: the crowd erupts and a centre-screen "GOAL!" banner shows the scoring side's flag + new score ([client/src/worldcup/matchHud.ts](../client/src/worldcup/matchHud.ts) `flashGoal`). When the goal doesn't end the Match the server snaps both players back to their kickoff spawns, re-centres the ball + keepers, pauses the Match clock, and **freezes movement for `WORLDCUP_MATCH_GOAL_RESET_MS` (default 5s)** while a "Kickoff in 3…2…1" countdown plays; `moveTo` is rejected server-side during the freeze and suppressed client-side ([server/src/rooms.ts](../server/src/rooms.ts) `worldcupHandleMatchGoal`/`worldcupKickoffReset`, [client/src/game/Game.ts](../client/src/game/Game.ts) `setWorldcupMoveLocked`)
- [x] **WS:** server→client `ballState` / `goalieState` / `goalScored` / `worldcupLeaderboard` / `matchState` / `matchEnded` (now with `aCountry`/`bCountry`) / `matchGoal` / `matchCountdown` / `matchPortalSpawn` / `matchPortalRemove` + `challengeOpen` & `worldcupCountry` on broadcast player state + `welcome` `balls` / `worldcupSelfCountry` / `worldcupTopCountries` / `worldcupPortals`; client→server `setCountry` / `placeBall` / `removeBall` / `setChallenge` / `acceptChallenge` / `leaveMatch` / `requestSpectate` ([client/src/net/ws.ts](../client/src/net/ws.ts))
- [x] Unit tests: [server/test/worldcup-ballPhysics.test.ts](../server/test/worldcup-ballPhysics.test.ts) (incl. blocker-mode keeper), [server/test/worldcup-scoreStore.test.ts](../server/test/worldcup-scoreStore.test.ts), [server/test/worldcup-goalReward.test.ts](../server/test/worldcup-goalReward.test.ts), [server/test/worldcup-match.test.ts](../server/test/worldcup-match.test.ts), [server/test/worldcup-goalie.test.ts](../server/test/worldcup-goalie.test.ts), [server/test/worldcup-matchPitch.test.ts](../server/test/worldcup-matchPitch.test.ts)

## Cosmetic shop

- [x] **Cosmetic Store** (SQLite in campaign DB) — catalog lifecycle (draft/published/archived), entitlements, loadout, changelog, admin grant ([`server/src/cosmeticStore.ts`](../server/src/cosmeticStore.ts))
- [x] **`/admin/cosmetics`** — preset picker, catalog CRUD, publish/archive, grant, changelog, **WebGL in-page preview** (same wardrobe VFX path as in-game), **Kenney particle library browser** ([`server/src/adminCosmeticsPage.ts`](../server/src/adminCosmeticsPage.ts), [`client/src/adminCosmeticPreview.ts`](../client/src/adminCosmeticPreview.ts))
- [x] **Kenney Particle Pack (CC0)** — shipped under [`client/public/assets/particles/kenney/`](../client/public/assets/particles/kenney/); v2 declarative prefab renderer ([`client/src/cosmetics/cosmeticPrefabFactory.ts`](../client/src/cosmetics/cosmeticPrefabFactory.ts), [`client/src/cosmetics/cosmeticPrefabRegistry.ts`](../client/src/cosmetics/cosmeticPrefabRegistry.ts)); reference presets `trail-ref-spark-path` + `aura-ref-magic-ring` with visual quality reviews ([`docs/cosmetics/visual-quality-rubric.md`](./cosmetics/visual-quality-rubric.md))
- [x] Player APIs — `GET /api/cosmetics/shop`, `GET /api/cosmetics/wardrobe` (includes a global **daily featured** shelf of up to 5 Published entries), `PUT /api/cosmetics/loadout`, `POST /api/cosmetics/unlock-intent`, `POST /api/cosmetics/unlock-sync`
- [x] **Payment intent** `nspace.cosmetic.unlock` — price locked at intent create; fulfill grants entitlement idempotently ([`server/src/cosmeticFulfill.ts`](../server/src/cosmeticFulfill.ts))
- [x] **Wardrobe** on self profile — paper-doll equip (Wardrobe + Shop tabs, slot dropdowns, **Wardrobe Preview**); read-only doll + Items strip on other profiles; public loadout on `GET /api/player-profile/:address` and in room player snapshots
- [x] **Player Menu Shop** — bottom-right menu drops **Profile**, adds **Shop** (full players; hidden for guests); both Wardrobe and Shop open the self profile overlay on their tab. **Closed by default** (`SHOP_ENABLED` / `VITE_SHOP_ENABLED` must be `1` to open): shows **COMING SOON**; when open, the Shop tab shows the global daily featured shelf (Buy / Owned / Equip; deployables noted "use from Action Wheel → Items") plus a link to **The Shaper** ([`client/src/ui/playerMenu.ts`](../client/src/ui/playerMenu.ts), [`client/src/cosmetics/wardrobePanel.ts`](../client/src/cosmetics/wardrobePanel.ts), [`client/src/cosmetics/shopAccess.ts`](../client/src/cosmetics/shopAccess.ts))
- [x] **The Shaper** — in-world cosmetic showroom, built-in room `cosmetic-gallery`, join code **SPACER**; reachable only when shop is open (`SHOP_ENABLED=1`); set `SHAPER_ENABLED=0` to hide the room entirely; not in the public room list; one in-world showcase per Preset (mannequins + deployable floor displays, trail pacing, floor plaques); server `cosmeticGallery` welcome payload + client showcase renderer ([`server/src/cosmeticGallery.ts`](../server/src/cosmeticGallery.ts))
- [x] **Shaper Return** — entering The Shaper records the previous room + approximate tile (sessionStorage, 30-min TTL); Player Menu **Leave the Shaper** and an in-world button send a server-validated `returnFromShaper` (room access re-checked, position must be walkable, Hub fallback)
- [x] **Action Wheel → Items** sector for owned deployables; arm → tap walkable tile (`deployCosmetic` WS intent)
- [x] **Deployables Allowed** room setting (dynamic rooms, default on) — [`roomRegistry.ts`](../server/src/roomRegistry.ts) v8

## Achievements (v1)

- [x] **Achievement registry** (code-defined) — onboarding, Commons build ladders, mining ladders; achievement points per milestone ([`server/src/achievementDefinitions.ts`](../server/src/achievementDefinitions.ts))
- [x] **Progress store** (campaign SQLite) — per-wallet counters + completions; idempotent unlock + cosmetic grant via `EntitlementSource` `achievement` ([`server/src/achievementStore.ts`](../server/src/achievementStore.ts))
- [x] **Dedicated reward SKUs** — `ach-*` trails in **Achievements** collection; excluded from shop purchase (`achievement_only`)
- [x] Server hooks — block place/mine, room enter/visit/create, loadout equip, client `achievementSignal` (profile, wardrobe, emote, signboard read)
- [x] APIs — `GET /api/achievements/me`; profile public fields `achievementPoints`, `achievementHighlights` on `GET /api/player-profile/:address`
- [x] WS — server→client `achievementUnlocked`; room broadcast `achievementCelebration` (trophy pop above earner); client→server `achievementSignal`
- [x] Client — **Achievements Window** with Category Navigator (sidebar on desktop, bottom drop-up on portrait), Summary view (recent + progress overview), per-category lists, reward swatches; opens via Player Menu or `Y`; profile AP + recent highlights, unlock toast; **Achievement Unlock Celebration** (in-world trophy spring above avatar, visible to room)
- [x] Tests — [`server/test/achievementStore.test.ts`](../server/test/achievementStore.test.ts)

## Achievements v3 (exploration & worldcraft)

- [x] **Exploration category** — Marathon I–III, Room Tourist, Grand Tour, Door Crasher, Teleporter Tourist, Outfield Explorer ([`server/src/explorationAchievementEvaluator.ts`](../server/src/explorationAchievementEvaluator.ts))
- [x] **Worldcraft category** — Palette Painter, Rainbow Floor, Architect's Toolkit, Prefab Author/Curator, Signpost Scribe/Reader, Gatekeeper, Trust Circle, Room Maker Deluxe ([`server/src/worldcraftAchievementEvaluator.ts`](../server/src/worldcraftAchievementEvaluator.ts))
- [x] Server hooks — floor recolor, terrain shapes, prefab publish/stamp, signpost place, gate open/ACL walk, furnished-room composite; `achievementSignal` `open_signboard` validated against signboard catalog

## Achievements v3 (World Cup extensions)

- [x] **Match extensions** — Clean Sheet, Comeback Kid, Golden Patience, Handshake Rival, Full Time, Own Goal Hero ([`server/src/matchAchievementEvaluator.ts`](../server/src/matchAchievementEvaluator.ts))
- [x] **Free Play Field extensions** — Rush Hour (≥4 players), Daily Streak (3 UTC days), Underdog Country ([`server/src/fieldGoalAchievementEvaluator.ts`](../server/src/fieldGoalAchievementEvaluator.ts))
- [x] Server hooks — match runtime tracks max deficit, own goals, golden phase; field goal leaderboard snapshot at credit time; mutual same-day challenge pairs via `achievement_daily_state`

## Client-only / polish

- [x] Identicon texture on spheres; fallback if load fails (e.g. NPC addresses)
- [x] Fog-of-war tuning via admin or constants

Unshipped / backlog ideas: [brainstorm/features-future.md](brainstorm/features-future.md).

Adjust this checklist when you ship features.
